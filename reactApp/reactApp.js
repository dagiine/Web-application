import React from 'react';
import ReactDOM from 'react-dom';

let elem = React.createElement("P", null, "hello reactJS");
let where = document.getElementById("reactApp");

ReactDOM.render(treeView, where);